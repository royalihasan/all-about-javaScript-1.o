/**
 * 
Variables in JavaScript
Vars in Js can be declared and define through 3 keyword

var : can be re-initialize and re-declare / Have no Scope In early js there were no scope
let : can be re-reassign and re-declare
const not reassign and redeclare


 */

const accountId = 0; // camelCase naming convention
const accountEmail = "royalihasan0987@gmail.com";
let userId;
console.log(accountEmail);

console.table([accountId, accountEmail , userId]);
